import 'package:flutter/material.dart';

void main(){
  runApp(MaterialApp(
    home: CatalogueJouet(),
  ));
}

class CatalogueJouet extends StatelessWidget {
  const CatalogueJouet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
